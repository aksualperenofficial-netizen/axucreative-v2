/** @type {import('next').NextConfig} */
const nextConfig = {
  // cPanel için static export (yorumu kaldırın)
  // output: 'export',
  // images: {
  //   unoptimized: true,
  // },
  // trailingSlash: true,
  reactStrictMode: true,
}

module.exports = nextConfig

